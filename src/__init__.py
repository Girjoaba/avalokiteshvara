"""Top-level package for the project.

In this repository the `src` directory is added to sys.path when running
scripts so that submodules (e.g. ``real_time``) can be imported using
absolute names.  This file is mostly here to allow editors and linters
to recognise the package.
"""