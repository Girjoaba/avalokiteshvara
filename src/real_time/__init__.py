"""Package marker for real_time components.

This directory contains modules used by the real-time pipeline and
robot simulation. The presence of this file makes Python treat the
folder as a package, allowing imports such as

    from real_time.advance_pipleine import move_pipeline

"""

# The package currently has no initialization logic.
